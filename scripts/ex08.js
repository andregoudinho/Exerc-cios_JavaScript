function ex08() {
    let x = 10;
    let y = 2;

    let diferenca = x - y;

    let resultado = (2 * x) + (3 * y);

    let multiplicacao = x * y;

    alert("A diferença das duas variáveis é: " + diferenca);
    alert("O dobro da primeira e o triplo da segunda é: " + resultado);
    alert("A multiplicação entre as duas é: " + multiplicacao);

}